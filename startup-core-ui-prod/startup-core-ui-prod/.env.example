# Startup Core UI - Environment Variables Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================
# The base URL of your backend API server
# Development: http://localhost:8000 (or your backend port)
# Staging: https://api-staging.yourdomain.com
# Production: https://api.yourdomain.com
VITE_WS_ENDPOINT=http://localhost:8000

# =============================================================================
# FILE UPLOAD CONFIGURATION
# =============================================================================
# Uploadcare Public Key for file upload functionality
# Sign up at: https://uploadcare.com/
# Get your public key from: https://app.uploadcare.com/projects/-/api-keys/
VITE_UCARE_PUBKEY=your_uploadcare_public_key_here

# =============================================================================
# DOCKER CONFIGURATION (Optional - only if using Docker)
# =============================================================================
# Docker Compose project name (used to prefix container names)
COMPOSE_PROJECT_NAME=start-up-core

# Port to expose the Docker container on (frontend will be accessible at this port)
# Default: 3000
COMPOSE_WEBSERVER_PORT=3000

# =============================================================================
# NOTES
# =============================================================================
# - This is a frontend-only application
# - Database connection is NOT configured here (handled by backend)
# - All VITE_* variables are exposed to the client-side code
# - Never commit .env file to version control
# - Keep your API keys secure
